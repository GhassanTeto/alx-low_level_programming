#include "main.h"
/**
 * _strlen -> function to get the length of a string
 * @s: string pointer tp passed to this function
 * Return: returns length of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
